// Canonical MLB accuracy URL. The implementation remains in the existing scorecard page
// so old /sim/accuracy links keep working while /accuracy/mlb becomes the public proof room.
export { default, revalidate } from "@/app/sim/accuracy-v2/page";
