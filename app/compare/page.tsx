import Link from "next/link";

import { getProofComparison, type ComparisonRow } from "@/services/proof/proof-comparison";

export const dynamic = "force-dynamic";
export const revalidate = 0;

function statusClass(status: ComparisonRow["status"]) {
  if (status === "LIVE") return "border-emerald-400/25 bg-emerald-400/10 text-emerald-200";
  if (status === "PARTIAL") return "border-amber-400/25 bg-amber-400/10 text-amber-200";
  return "border-slate-400/25 bg-slate-400/10 text-slate-200";
}

function Stat({ label, value, note }: { label: string; value: string; note: string }) {
  return <div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">{label}</div><div className="mt-2 font-display text-3xl font-black tracking-tight text-white">{value}</div><div className="mt-2 text-xs leading-5 text-slate-400">{note}</div></div>;
}

export default async function ComparePage() {
  const comparison = await getProofComparison();
  return <main className="min-h-screen bg-[#02060b] px-3 py-4 text-white sm:px-6"><div className="mx-auto grid max-w-7xl gap-5 pb-16"><section className="rounded-[1.75rem] border border-cyan-300/15 bg-slate-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[10px] font-black uppercase tracking-[0.28em] text-cyan-300/75">Proof Comparison</div><h1 className="mt-2 font-display text-4xl font-black tracking-tight text-white sm:text-5xl">Pick cards are not enough</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">SharkEdge is being built around receipts, visible misses, public record lanes, audit checks, CSV export, and live badges. This page converts that moat into a direct product comparison.</p></div><div className="flex gap-2"><Link href="/api/compare" className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">JSON</Link><Link href="/record" className="rounded-full border border-cyan-300/35 bg-cyan-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-cyan-100">Record</Link><Link href="/badges" className="rounded-full border border-emerald-300/35 bg-emerald-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-emerald-100">Badges</Link></div></div></section><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Stat label="Record" value={comparison.headline.record} note="From public result lanes." /><Stat label="Units" value={comparison.headline.units} note="Actual/fallback odds separated." /><Stat label="ROI" value={comparison.headline.roi} note="Sample-sensitive, shown with caution." /><Stat label="Tickets" value={String(comparison.headline.tickets)} note="Locked receipts generated." /><Stat label="Audit" value={`${comparison.headline.auditScore}%`} note="Transparency manifest score." /></div><section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-4 sm:p-5"><div className="grid gap-3"><div className="grid grid-cols-1 gap-3 rounded-2xl border border-white/10 bg-black/25 p-3 text-[10px] font-black uppercase tracking-[0.18em] text-slate-500 md:grid-cols-[1fr_1.2fr_1.2fr_7rem]"><div>Capability</div><div>Generic pick-card product</div><div>SharkEdge</div><div>Status</div></div>{comparison.rows.map((row) => <div key={row.capability} className="grid grid-cols-1 gap-3 rounded-[1.15rem] border border-white/10 bg-[#06101b]/82 p-4 md:grid-cols-[1fr_1.2fr_1.2fr_7rem]"><div className="font-black text-white">{row.capability}</div><div className="text-sm leading-6 text-slate-400">{row.pickCardProduct}</div><div className="text-sm leading-6 text-cyan-100">{row.sharkEdge}</div><div><span className={`rounded-full border px-3 py-1 text-[10px] font-black uppercase tracking-[0.14em] ${statusClass(row.status)}`}>{row.status}</span></div></div>)}</div></section><section className="grid gap-3 md:grid-cols-2"><div className="rounded-[1.35rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-cyan-300">Public routes</div><div className="mt-4 grid gap-2">{Object.entries(comparison.publicRoutes).map(([key, href]) => <Link key={key} href={href} className="rounded-2xl border border-white/10 bg-black/20 p-3 font-mono text-xs text-cyan-100 transition hover:border-cyan-300/35">{href}</Link>)}</div></div><div className="rounded-[1.35rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-amber-300">Warnings</div><div className="mt-4 grid gap-2">{comparison.warnings.length ? comparison.warnings.map((warning) => <div key={warning} className="rounded-2xl border border-amber-400/15 bg-amber-400/5 p-3 text-xs leading-5 text-amber-100">{warning}</div>) : <div className="rounded-2xl border border-white/10 bg-black/20 p-3 text-xs leading-5 text-slate-400">No warnings returned.</div>}</div></div></section></div></main>;
}
