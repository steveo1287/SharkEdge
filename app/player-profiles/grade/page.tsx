import Link from "next/link";

import { getMlbPlayerCardGrade, type MlbPlayerCardGradeCheck } from "@/services/players/mlb-player-card-grade";

export const dynamic = "force-dynamic";
export const revalidate = 0;

function statusClass(status: MlbPlayerCardGradeCheck["status"]) {
  if (status === "PASS") return "border-emerald-400/25 bg-emerald-400/10 text-emerald-200";
  if (status === "WARN") return "border-amber-400/25 bg-amber-400/10 text-amber-200";
  return "border-red-400/25 bg-red-400/10 text-red-200";
}

function Stat({ label, value, note }: { label: string; value: string; note: string }) {
  return <div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">{label}</div><div className="mt-2 font-display text-3xl font-black tracking-tight text-white">{value}</div><div className="mt-2 text-xs leading-5 text-slate-400">{note}</div></div>;
}

export default async function PlayerCardGradePage() {
  const grade = await getMlbPlayerCardGrade();
  return <main className="min-h-screen bg-[#02060b] px-3 py-4 text-white sm:px-6"><div className="mx-auto grid max-w-7xl gap-5 pb-16"><section className="rounded-[1.75rem] border border-cyan-300/15 bg-slate-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[10px] font-black uppercase tracking-[0.28em] text-cyan-300/75">MLB Player Card Grade</div><h1 className="mt-2 font-display text-4xl font-black tracking-tight text-white sm:text-5xl">A+ readiness board</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">Grades the player-card system across coverage, freshness, trait depth, model-impact coverage, market-driver coverage, driver quality, and ticket wiring.</p></div><div className="flex gap-2"><Link href="/api/mlb/player-cards/grade" className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">JSON</Link><Link href="/player-profiles/drivers" className="rounded-full border border-cyan-300/35 bg-cyan-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-cyan-100">Drivers</Link><Link href="/player-profiles" className="rounded-full border border-emerald-300/35 bg-emerald-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-emerald-100">Profiles</Link></div></div></section><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Stat label="Grade" value={grade.grade} note={`Target ${grade.target}. Gap ${grade.gapToAPlus} points.`} /><Stat label="Score" value={`${grade.score}/100`} note="Weighted system score." /><Stat label="Profiles" value={String(grade.summary.profiles)} note={`${grade.summary.batters} bats, ${grade.summary.starters} starters.`} /><Stat label="Driver markets" value={`${grade.summary.activeDriverMarkets}/7`} note="Markets with active ranked drivers." /><Stat label="Ticket wiring" value={`${grade.summary.ticketEdgeHitRate.toFixed(1)}%`} note="Sampled tickets with player edges." /></section><section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-cyan-300">Readiness checks</div><div className="mt-4 grid gap-3">{grade.checks.map((check) => <div key={check.key} className="rounded-[1.15rem] border border-white/10 bg-[#06101b]/82 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="font-black text-white">{check.label}</div><p className="mt-2 text-sm leading-6 text-slate-400">{check.detail}</p></div><div className="flex items-center gap-2"><span className={`rounded-full border px-3 py-1 text-[10px] font-black uppercase tracking-[0.14em] ${statusClass(check.status)}`}>{check.status}</span><span className="font-display text-3xl font-black text-white">{check.score}</span></div></div></div>)}</div></section><section className="grid gap-4 lg:grid-cols-2"><div className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-emerald-300">Market coverage</div><div className="mt-4 grid gap-2">{grade.markets.map((market) => <Link key={market.market} href={`/player-profiles/drivers?market=${market.market}`} className="rounded-2xl border border-white/10 bg-black/20 p-3 transition hover:border-cyan-300/35"><div className="flex items-center justify-between gap-3"><div><div className="font-black text-white">{market.market}</div><div className="mt-1 text-xs text-slate-500">Top: {market.topDriver ?? "—"}</div></div><div className="text-right"><div className="font-display text-2xl font-black text-white">{market.drivers}</div><div className="font-mono text-xs text-emerald-200">{market.topScore ?? "—"}</div></div></div></Link>)}</div></div><div className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-amber-300">Next build priorities</div><div className="mt-4 grid gap-2">{grade.nextBuildPriorities.map((item) => <div key={item.key} className="rounded-2xl border border-amber-400/15 bg-amber-400/5 p-3"><div className="font-black text-white">{item.label}</div><div className="mt-1 text-xs text-amber-100">Current {item.currentScore}. Needs +{item.needed} to reach A-range.</div></div>)}{!grade.nextBuildPriorities.length ? <div className="rounded-2xl border border-emerald-400/15 bg-emerald-400/5 p-3 text-sm text-emerald-100">No weak priorities under 92.</div> : null}</div></div></section>{grade.warnings.length ? <section className="grid gap-2">{grade.warnings.map((warning) => <div key={warning} className="rounded-2xl border border-amber-400/15 bg-amber-400/5 p-3 text-xs leading-5 text-amber-100">{warning}</div>)}</section> : null}</div></main>;
}
