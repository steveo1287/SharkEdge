import Link from "next/link";

import { getConfiguredMlbPlayerDataSources } from "@/services/players/mlb-player-data-source-sync";

export const dynamic = "force-dynamic";
export const revalidate = 0;

function Stat({ label, value, note }: { label: string; value: string; note: string }) {
  return <div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">{label}</div><div className="mt-2 font-display text-3xl font-black tracking-tight text-white">{value}</div><div className="mt-2 text-xs leading-5 text-slate-400">{note}</div></div>;
}

export default function PlayerSourceSyncPage() {
  const sources = getConfiguredMlbPlayerDataSources();
  return <main className="min-h-screen bg-[#02060b] px-3 py-4 text-white sm:px-6"><div className="mx-auto grid max-w-7xl gap-5 pb-16"><section className="rounded-[1.75rem] border border-cyan-300/15 bg-slate-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[10px] font-black uppercase tracking-[0.28em] text-cyan-300/75">MLB Player Stat Source Sync</div><h1 className="mt-2 font-display text-4xl font-black tracking-tight text-white sm:text-5xl">Feed adapters into the guarded pipe</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">Configured stat URLs are fetched, normalized, validated, ingested through the guarded gate, then optionally calibrated into realistic player-card ratings.</p></div><div className="flex gap-2"><Link href="/api/mlb/player-data/source-sync" className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">Run JSON</Link><Link href="/player-profiles/data-quality" className="rounded-full border border-cyan-300/35 bg-cyan-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-cyan-100">Quality</Link><Link href="/player-profiles/calibration" className="rounded-full border border-emerald-300/35 bg-emerald-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-emerald-100">Calibration</Link></div></div></section><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Sources" value={String(sources.length)} note="Configured stat feeds." /><Stat label="Combined" value={String(sources.filter((s) => s.kind === "combined").length)} note="Feeds with batters and pitchers." /><Stat label="Batter feeds" value={String(sources.filter((s) => s.kind === "batters").length)} note="Batter-only stat feeds." /><Stat label="Pitcher feeds" value={String(sources.filter((s) => s.kind === "pitchers").length)} note="Pitcher-only stat feeds." /></section>{!sources.length ? <section className="rounded-[1.5rem] border border-amber-400/15 bg-amber-400/5 p-5"><div className="font-black text-amber-100">No stat URLs configured.</div><p className="mt-2 text-sm leading-6 text-amber-100/80">Set one or more Railway variables: MLB_PLAYER_STATS_URLS, MLB_BATTER_STATS_URLS, MLB_PITCHER_STATS_URLS. Comma-separated URLs are supported.</p></section> : null}<section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-cyan-300">Configured sources</div><div className="mt-4 grid gap-3">{sources.map((source) => <div key={`${source.kind}-${source.url}`} className="rounded-[1.15rem] border border-white/10 bg-[#06101b]/82 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="text-[10px] font-black uppercase tracking-[0.16em] text-cyan-300">{source.kind}</div><div className="mt-1 font-black text-white">{source.name}</div><div className="mt-1 break-all text-xs text-slate-500">{source.url}</div></div><span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-1 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">configured</span></div></div>)}</div></section><section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-emerald-300">Production sync call</div><pre className="mt-4 overflow-x-auto rounded-2xl border border-white/10 bg-black/40 p-4 text-xs leading-6 text-slate-300">{`POST /api/mlb/player-data/source-sync
{
  "calibrate": true,
  "season": 2026,
  "source": "calibrated-stat-pipe-v1"
}`}</pre></section><section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-amber-300">Accepted feed shape</div><pre className="mt-4 overflow-x-auto rounded-2xl border border-white/10 bg-black/40 p-4 text-xs leading-6 text-slate-300">{`{
  "batters": [{ "playerId": "...", "playerName": "...", "team": "NYY", "season": 2026, "avg": .280, "obp": .360, "slg": .510, "kRate": .24 }],
  "pitchers": [{ "pitcherId": "...", "pitcherName": "...", "team": "NYY", "season": 2026, "xera": 3.40, "fip": 3.60, "kRate": .28 }]
}`}</pre></section></div></main>;
}
