import Link from "next/link";

import { getProofBadgeData } from "@/services/proof/proof-badge";

export const dynamic = "force-dynamic";
export const revalidate = 0;

function Code({ value }: { value: string }) {
  return <pre className="overflow-x-auto rounded-2xl border border-white/10 bg-black/35 p-4 text-xs leading-6 text-cyan-100"><code>{value}</code></pre>;
}

export default async function BadgesPage() {
  const badge = await getProofBadgeData();
  const markdown = `[![SharkEdge Proof](/api/badges/record)](/record)`;
  const html = `<a href="/record"><img src="/api/badges/record" alt="SharkEdge Proof" /></a>`;

  return <main className="min-h-screen bg-[#02060b] px-3 py-4 text-white sm:px-6"><div className="mx-auto grid max-w-6xl gap-5 pb-16"><section className="rounded-[1.75rem] border border-cyan-300/15 bg-slate-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[10px] font-black uppercase tracking-[0.28em] text-cyan-300/75">Proof Badges</div><h1 className="mt-2 font-display text-4xl font-black tracking-tight text-white sm:text-5xl">Embeddable record receipts</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">A live SVG badge built from the public record board. Use it anywhere that needs a compact proof receipt: docs, landing pages, posts, or partner pages.</p></div><div className="flex gap-2"><Link href="/api/badges/record" className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">SVG</Link><Link href="/api/badges/record?format=json" className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-slate-300">JSON</Link><Link href="/record" className="rounded-full border border-cyan-300/35 bg-cyan-300/10 px-3 py-2 text-[10px] font-black uppercase tracking-[0.14em] text-cyan-100">Record</Link></div></div></section><section className="rounded-[1.5rem] border border-white/10 bg-slate-950/75 p-5"><div className="text-[10px] font-black uppercase tracking-[0.2em] text-emerald-300">Live badge</div><div className="mt-5 overflow-hidden rounded-[1.25rem] border border-white/10 bg-black/30 p-4"><img src="/api/badges/record" alt="SharkEdge proof badge" className="h-auto w-full max-w-[760px]" /></div></section><section className="grid gap-3 md:grid-cols-4"><div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">Record</div><div className="mt-2 font-display text-3xl font-black text-white">{badge.record}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">Units</div><div className="mt-2 font-display text-3xl font-black text-white">{badge.units}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">ROI</div><div className="mt-2 font-display text-3xl font-black text-white">{badge.roi}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.035] p-4"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-slate-500">Tickets</div><div className="mt-2 font-display text-3xl font-black text-white">{badge.tickets}</div></div></section><section className="grid gap-4"><div><div className="mb-2 text-[10px] font-black uppercase tracking-[0.2em] text-cyan-300">Markdown</div><Code value={markdown} /></div><div><div className="mb-2 text-[10px] font-black uppercase tracking-[0.2em] text-cyan-300">HTML</div><Code value={html} /></div></section></div></main>;
}
