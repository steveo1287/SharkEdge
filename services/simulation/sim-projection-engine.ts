import { buildNbaIntel, type NbaIntelResult } from "@/services/simulation/nba-intelligence-engine";
import { calibrateNbaAgainstMarket } from "@/services/simulation/nba-market-calibration";
import type { LeagueKey, ScoreboardPreviewView } from "@/lib/types/domain";

export type SimGame = ScoreboardPreviewView & { leagueKey: LeagueKey; leagueLabel: string };
export type Confidence = "A" | "B" | "C";
export type Pace = "Fast" | "Neutral" | "Slow";
export type Volatility = "High" | "Medium" | "Low";
export type UpsetRisk = "High" | "Medium" | "Low";
export type InsightTone = "strong" | "watch" | "avoid" | "neutral";
export type SmartInsight = { label: string; tone: InsightTone; detail: string };
export type SimDistribution = { runs: number; homeWinPct: number; awayWinPct: number; avgHome: number; avgAway: number; avgTotal: number; medianTotal: number; medianSpreadHome: number; homeBlowoutPct: number; awayBlowoutPct: number; oneScorePct: number; totalLow: number; totalHigh: number };
export type SimProjection = { matchup: { away: string; home: string }; homeScore: number; awayScore: number; homeWinPct: number; awayWinPct: number; total: number; spreadHome: number; confidence: Confidence; lean: string; pace: Pace; volatility: Volatility; upsetRisk: UpsetRisk; totalBand: { low: number; high: number }; spreadBand: { low: number; high: number }; modelTags: string[]; read: string; distribution: SimDistribution; insights: SmartInsight[]; nbaIntel?: NbaIntelResult };
type SimOutcome = { homeScore: number; awayScore: number; total: number; spreadHome: number; homeWin: boolean };
const BASE_SCORING: Partial<Record<LeagueKey, { home: number; away: number; variance: number; baselineTotal: number }>> = { NBA: { home: 113, away: 109, variance: 8, baselineTotal: 222 }, MLB: { home: 5, away: 4, variance: 2, baselineTotal: 9 }, NHL: { home: 3.2, away: 2.9, variance: 1.2, baselineTotal: 6.1 }, NFL: { home: 24, away: 21, variance: 6, baselineTotal: 45 }, NCAAF: { home: 31, away: 27, variance: 9, baselineTotal: 58 }, UFC: { home: 1, away: 1, variance: 1, baselineTotal: 2 }, BOXING: { home: 1, away: 1, variance: 1, baselineTotal: 2 } };
function hashString(value: string) { let hash = 0; for (let i = 0; i < value.length; i += 1) hash = (hash * 31 + value.charCodeAt(i)) >>> 0; return hash; }
function seedUnit(seed: number) { return (seed % 1000) / 1000; }
function seededOffset(seed: number, range: number) { return (seedUnit(seed) - 0.5) * range * 2; }
function parseMatchup(label: string) { const [away, home] = label.split(" @ ").map((part) => part?.trim()).filter(Boolean); return { away: away ?? "Away", home: home ?? "Home" }; }
function roundScore(value: number, leagueKey: LeagueKey) { return leagueKey === "MLB" || leagueKey === "NHL" ? Math.max(0, Number(value.toFixed(1))) : Math.max(0, Math.round(value)); }
function roundModelValue(value: number, leagueKey: LeagueKey) { return leagueKey === "MLB" || leagueKey === "NHL" ? Number(value.toFixed(1)) : Math.round(value); }
function median(values: number[]) { const sorted = [...values].sort((a, b) => a - b); return sorted[Math.floor(sorted.length / 2)] ?? 0; }
function pct(count: number, total: number) { return total ? count / total : 0; }
function confidenceFromScore(score: number, volatility: Volatility, upsetRisk: UpsetRisk): Confidence { if (score >= 68 && volatility !== "High" && upsetRisk !== "High") return "A"; if (score <= 45 || volatility === "High" || upsetRisk === "High") return "C"; return "B"; }
function buildDistribution(args: { game: SimGame; homeScore: number; awayScore: number; varianceSeed: number; volatilityMultiplier: number }): SimDistribution { const base = BASE_SCORING[args.game.leagueKey] ?? { home: 24, away: 21, variance: 6, baselineTotal: 45 }; const runs = 100; const outcomes: SimOutcome[] = Array.from({ length: runs }, (_, index) => { const runSeed = hashString(`${args.game.id}:${index}:projection-engine`); const vol = (0.7 + args.varianceSeed * 0.9) * base.variance * args.volatilityMultiplier; const home = roundScore(args.homeScore + seededOffset(runSeed, vol), args.game.leagueKey); const away = roundScore(args.awayScore + seededOffset(runSeed >>> 4, vol), args.game.leagueKey); const total = Number((home + away).toFixed(1)); const spreadHome = Number((home - away).toFixed(1)); return { homeScore: home, awayScore: away, total, spreadHome, homeWin: spreadHome >= 0 }; }); const totals = outcomes.map((outcome) => outcome.total); const spreads = outcomes.map((outcome) => outcome.spreadHome); const homeWins = outcomes.filter((outcome) => outcome.homeWin).length; const blowoutLine = args.game.leagueKey === "MLB" || args.game.leagueKey === "NHL" ? 2 : 10; const oneScoreLine = args.game.leagueKey === "MLB" || args.game.leagueKey === "NHL" ? 1.5 : 7; return { runs, homeWinPct: pct(homeWins, runs), awayWinPct: pct(runs - homeWins, runs), avgHome: Number((outcomes.reduce((sum, outcome) => sum + outcome.homeScore, 0) / runs).toFixed(1)), avgAway: Number((outcomes.reduce((sum, outcome) => sum + outcome.awayScore, 0) / runs).toFixed(1)), avgTotal: Number((outcomes.reduce((sum, outcome) => sum + outcome.total, 0) / runs).toFixed(1)), medianTotal: median(totals), medianSpreadHome: median(spreads), homeBlowoutPct: pct(outcomes.filter((outcome) => outcome.spreadHome >= blowoutLine).length, runs), awayBlowoutPct: pct(outcomes.filter((outcome) => outcome.spreadHome <= -blowoutLine).length, runs), oneScorePct: pct(outcomes.filter((outcome) => Math.abs(outcome.spreadHome) <= oneScoreLine).length, runs), totalLow: Math.min(...totals), totalHigh: Math.max(...totals) }; }
function buildInsights(args: { projection: Omit<SimProjection, "insights"> }): SmartInsight[] { const p = args.projection; const insights: SmartInsight[] = []; if (p.nbaIntel) { insights.push({ label: `${p.nbaIntel.factorStrength.toUpperCase()} NBA intel profile`, tone: p.nbaIntel.confidenceScore >= 61 ? "strong" : p.nbaIntel.confidenceScore <= 45 ? "avoid" : "watch", detail: `Model confidence ${p.nbaIntel.confidenceScore}/100 with projected home edge ${p.nbaIntel.projectedHomeEdge}.` }); const topFactor = [...p.nbaIntel.factors].sort((a, b) => Math.abs(b.contribution) - Math.abs(a.contribution))[0]; if (topFactor) insights.push({ label: topFactor.label, tone: topFactor.contribution >= 0 ? "strong" : "watch", detail: `${topFactor.explanation} Contribution: ${topFactor.contribution > 0 ? "+" : ""}${topFactor.contribution}.` }); }
  if ((p.distribution.homeWinPct >= 0.58 || p.distribution.awayWinPct >= 0.58) && p.confidence !== "C") insights.push({ label: "Strong directional lean", tone: "strong", detail: `${p.lean} wins a clear majority of the 100-run distribution.` }); if (p.volatility === "High" || p.distribution.oneScorePct >= 0.48) insights.push({ label: "Chaos / tight-game profile", tone: "watch", detail: "High volatility or tight-game frequency makes this a lower-certainty setup." }); if (p.volatility === "High" && p.confidence === "C") insights.push({ label: "Avoid forcing it", tone: "avoid", detail: "Model spread is wide and confidence is low. Better as a watchlist game." }); if (!insights.length) insights.push({ label: "Neutral research spot", tone: "neutral", detail: "No major edge or danger flag. Use as baseline context." }); return insights.slice(0, 4); }
export async function buildSimProjection(game: SimGame): Promise<SimProjection> { const matchup = parseMatchup(game.label); const base = BASE_SCORING[game.leagueKey] ?? { home: 24, away: 21, variance: 6, baselineTotal: 45 }; const seed = hashString(`${game.id}:${game.leagueKey}:${game.label}:${game.startTime}`); const nbaIntel = game.leagueKey === "NBA" ? await buildNbaIntel(matchup.away, matchup.home) : undefined; const genericPaceSeed = seedUnit(seed >>> 5); const genericFormSwing = (seedUnit(seed >>> 9) - 0.5) * base.variance; const nbaHomeEdge = nbaIntel?.projectedHomeEdge ?? 0; const totalShift = nbaIntel?.projectedTotalShift ?? 0; const volatilityMultiplier = nbaIntel?.volatilityIndex ?? 1; const paceIndex = nbaIntel ? totalShift / 3 : genericPaceSeed > 0.68 ? 2 : genericPaceSeed < 0.32 ? -2 : 0; const paceMultiplier = 1 + paceIndex / 100; const homeRaw = base.home * paceMultiplier + 1.2 + genericFormSwing * 0.25 + totalShift / 2 + nbaHomeEdge * 0.58; const awayRaw = base.away * paceMultiplier - genericFormSwing * 0.18 + totalShift / 2 - nbaHomeEdge * 0.36; const homeScore = roundScore(homeRaw, game.leagueKey); const awayScore = roundScore(awayRaw, game.leagueKey); const spreadHome = Number((homeScore - awayScore).toFixed(1)); const total = Number((homeScore + awayScore).toFixed(1)); const market = game.leagueKey === "NBA" ? await calibrateNbaAgainstMarket({ awayTeam: matchup.away, homeTeam: matchup.home, modelSpreadHome: spreadHome, modelTotal: total }) : undefined; const adjustedConfidence = Math.max(20, Math.min(95, (nbaIntel?.confidenceScore ?? 50) + (market?.marketConfidenceAdjustment ?? 0))); const varianceSeed = Math.max(0, Math.min(1, seedUnit(seed >>> 13) + (volatilityMultiplier - 1) * 0.22)); const distribution = buildDistribution({ game, homeScore, awayScore, varianceSeed, volatilityMultiplier }); const volatility: Volatility = volatilityMultiplier >= 1.24 || varianceSeed > 0.72 ? "High" : volatilityMultiplier <= 0.92 && varianceSeed < 0.42 ? "Low" : "Medium"; const pace: Pace = paceIndex > 1.2 ? "Fast" : paceIndex < -1.2 ? "Slow" : "Neutral"; const upsetRisk: UpsetRisk = Math.abs(spreadHome) <= base.variance * 0.35 ? "High" : Math.abs(spreadHome) <= base.variance * 0.7 ? "Medium" : "Low"; const confidence = confidenceFromScore(adjustedConfidence, volatility, upsetRisk); const homeWinPct = Math.max(0.29, Math.min(0.78, distribution.homeWinPct)); const bandWidth = (volatility === "High" ? base.variance * 1.3 : volatility === "Low" ? base.variance * 0.58 : base.variance * 0.88) * volatilityMultiplier; const withoutInsights: Omit<SimProjection, "insights"> = { matchup, homeScore, awayScore, homeWinPct, awayWinPct: 1 - homeWinPct, total, spreadHome, confidence, lean: spreadHome >= 0 ? matchup.home : matchup.away, pace, volatility, upsetRisk, totalBand: { low: roundModelValue(total - bandWidth, game.leagueKey), high: roundModelValue(total + bandWidth, game.leagueKey) }, spreadBand: { low: roundModelValue(spreadHome - bandWidth * 0.45, game.leagueKey), high: roundModelValue(spreadHome + bandWidth * 0.45, game.leagueKey) }, modelTags: ["100-run sim", nbaIntel ? nbaIntel.modelVersion : "generic", market?.signal ?? "no-market"], read: nbaIntel ? `${nbaIntel.notes.join(" ")} ${market?.notes.join(" ") ?? ""}` : "Model read unavailable.", distribution, nbaIntel }; return { ...withoutInsights, insights: buildInsights({ projection: withoutInsights }) }; }
