# Main test wire warning

The focused elite rating test exists, but the root `package.json` test chain has not been updated in this pass.

Manual command:

```bash
npx tsx tests/mlb-elite-rating-system.test.ts
```

Add it to the root test chain after it passes locally with the full repo environment.
